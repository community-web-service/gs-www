"use strict";

// @future Include comments that explains what each of these does.
// @future Consider converting this to XML.
module.exports = {
	wwwBase: "./src/www",
	defaultGlobOptions: {
		strict: true,
		dot: true,
		follow: true
	}
};
